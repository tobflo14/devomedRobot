#version 330 core

// input data
in vec3 fragmentColor;

// Ouput data
out vec3 color;

uniform vec4 uniformcolor;

// live plot test
//in vec4 color;
//out vec4 outputColor;


void main()
{

	// Output color = red 
	//color = vec3(1,0,1);
	//color = fragmentColor;
	//outputColor = color;
	gl_FragColor = uniformcolor;

}